# Runtime dependencies for the Electronic Lab Notebook
# Python 3.9+ required

# API server
Flask==3.0.0
flask-cors==4.0.0

# Config parsing (sdgl.toml). tomllib is stdlib on Python 3.11+;
# tomli is the backport for 3.9 / 3.10.
tomli==2.0.1 ; python_version < "3.11"

# Notebook (.ipynb) reports: canonical read/write so admin edits of markdown
# cells round-trip without reformatting the rest of the notebook.
nbformat>=5.9

# --- Optional / future (kept commented until the relevant step lands) ---
# pillow>=10.0.0       # thumbnail generation
# rfc3161ng>=2.1.3     # RFC 3161 trusted timestamps (compliance layer, Phase D)
